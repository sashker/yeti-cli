#!/usr/bin/make -f

%:
	dh $@ --with python2


override_dh_builddeb:
	sed -i -e "s/\(__version__ =\).*/\1 \"`dpkg-parsechangelog --show-field version`\"/" debian/yeti-cli/usr/sbin/yeti-cli
	dh_builddeb
